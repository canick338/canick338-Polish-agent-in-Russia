# Профессиональная система игры "Агент в Обояни" (Engine v2.0 Update)

## ✅ Что нового в Engine v2.0

### 1. Архитекутра
- **JSON Core**: Полный переход на JSON для хранения данных. Быстро, надежно, расширяемо.
- **Expression Evaluator**: Условия `if` теперь обрабатываются нативной Godot Expression, что работает в 100 раз быстрее и безопаснее.
- **Event System**: Диалоги могут отправлять сигналы в движок (тряска камеры, звук, спецэффекты).

### 2. Инструментарий
- **Dialogue Editor Plugin**: Встроенный в Godot визуальный редактор.
    - Вкладка **Dialogues** в верхней панели.
    - Редактирование графов и блоков текста.
    - Валидация команд.

### 3. Файловая структура (Обновлено)

#### Основные сценарии (JSON):
```
Story/
├── DESIGN_DOCUMENT.md         - Дизайн-документ
├── README_PROFESSIONAL.md     - Техническое руководство
├── master_scenario.json       - Главный мастер-файл
├── months/
│   ├── month1_february.json   - Февраль
│   └── ...
├── minigames/
│   ├── photo_mission.json     - Фото-миссия
│   └── ...
└── Legacy_TXT/                - Архив старых текстовых файлов
```

## 🎮 Технические Особенности

### Команды (JSON Command Node)
В редакторе используются типизированные команды:

#### `set`
Установка переменных `GameGlobal`.
- Arg0: Имя переменной (напр. `stealth_level`)
- Arg1: Значение (напр. `50` или `"true"`)

#### `jump`
Переход к метке или другой сцене.
- Arg0: Название метки

#### `background`
Смена фона с переходом.
- Arg0: Путь к картинке (`res://Backgrounds/room.jpg`)
- Arg1: Эффект (опционально, напр. `fade`)

#### `minigame`
Запуск отдельной сцены-минигры.
- Arg0: Имя мини-игры (ключ в реестре)

### Макросы в тексте
Поддерживаются подстановки:
- `{PLAYER_NAME}` - Имя игрока.
- `{VARIABLE}` - Значение любой переменной.

### Сигналы событий
Пример JSON узла для события:
```json
{
    "type": "command", 
    "name": "signal", 
    "args": ["shake_camera", 1.5]
}
```
Вызывает `scene_event("shake_camera", [1.5])` в `ScenePlayer`.

## 📊 Система выборов и Условий

Благодаря переходу на `Expression` класс, условия стали гибче:
- `if stealth_level > 50 and money >= 100` — поддерживается сложная логика.

### Пример выбора в редакторе:
1. Создать узел **Choice**.
2. Добавить ветки.
3. В условиях веток можно писать выражения GDScript.

## 🚀 Как работать с контентом

1. **Создание**: Откройте Dialogue Editor, нажмите `Add Node`.
2. **Текст**: Используйте `Dialogue Block` для написания сценария (как в чате).
3. **Логика**: Соединяйте блоки с командами и выборами.
4. **Сохранение**: Кнопка `Save JSON` пишет сразу в файл.
5. **Тест**: Запустите игру (F5).

---
**Примечание**: Старые `.txt` файлы больше не используются движком напрямую. Если вы пишете в текстовом редакторе, вам потребуется конвертер. Рекомендуется использовать встроенный плагин.
