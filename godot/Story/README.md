# Структура сюжета игры (Engine v2.0)

## 🆕 Новая система диалогов (JSON)

Игра переведена на современный JSON-формат. Старые `.txt` файлы находятся в папке `Legacy_TXT` для архива.

### Главные файлы:
- `master_scenario.json` - главная структура, заменяет main_game_loop
- `prequel_danila.json` - предыстория
- `act1_arrival.json` - Акт 1
- `act2_infiltration.json` - Акт 2
- `act3_danger.json` - Акт 3
- `endings.json` - Концовки

## 🛠️ Визуальный Редактор (Dialogue Editor)

В проект добавлен плагин для удобного редактирования сценариев.

### Как включить:
1. Зайти в **Project Settings -> Plugins**.
2. Включить **DialogueEditor**.
3. Перейти на вкладку **Dialogues** (рядом с 2D/3D/Script).

### Возможности:
- **Visual Graph**: Редактирование сцен в виде графа.
- **Dialogue Blocks**: Линейные диалоги группируются в блоки `Имя: Текст`.
- **Commands**: Удобные формы для смены фона, запуска мини-игр и изменения переменных.
- **Save JSON**: Прямое сохранение в файлы игры.

## Новые Фичи

### 1. Rich Text Macros
В тексте можно использовать макросы:
- `{PLAYER_NAME}` -> подставится имя игрока
- `{KARMA}` -> значение переменной
- `[COLOR=red]Text[/COLOR]` -> BBCode работает как обычно.

### 2. Events (Сигналы)
Диалоги могут вызывать события движка:
- **Camera Shake**: `signal: { "name": "shake", "args": [1.0] }`
- **Sound**: `signal: { "name": "play_sfx", "args": ["gunshot"] }`

## Порядок загрузки

1. **Казино** - запускается первым.
2. **Пролог/Предыстория** - загружается через `Main.gd`.
3. **Акты 1-3** - последовательно.

## Переменные (GameGlobal)

Используйте команду `Set Variable` в редакторе:
- `suspicion_level` (0-100)
- `friendship_anna` (0-100)
- `friendship_dima` (0-100)
- `stealth_level` (0-100)

## Концовки

1. **Хороший финал**: suspicion_level < 40, миссия выполнена.
2. **Средний финал**: suspicion_level 40-70, побег.
3. **Плохой финал**: suspicion_level > 70, арест.
4. **Секретный финал**: особый выбор в Акте 3.

## Как добавить новую сцену

1. Создайте новый `.json` файл в папке `Story` (или через редактор).
2. Добавьте его в список `scripts` в `Main.tscn` (через Инспектор).
   *Теперь движок автоматически понимает JSON файлы.*

## Ресурсы

### Фоны:
Находятся в `res://Backgrounds/`. Используйте команду `Change Background` в редакторе, указывая путь.

### Персонажи:
Настраиваются в `res://Characters/`. В диалогах используйте их ID (например, `marek`, `anna`).
