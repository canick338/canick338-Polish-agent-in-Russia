# Анимированные портреты персонажей

Здесь хранятся короткие видео-анимации для портретов персонажей.
Эти анимации используются ВО ВРЕМЯ ДИАЛОГОВ, а не как полноэкранные кат-сцены.

## 📁 Структура папок:

```
Animations/
├── Danila/
│   ├── nervous.ogv          # Нервничает
│   ├── happy.ogv            # Радуется
│   ├── thinking.ogv         # Думает
│   ├── angry.ogv            # Злится
│   └── scared.ogv           # Испуган
├── Anna/
│   ├── smile.ogv
│   ├── laugh.ogv
│   └── blush.ogv
├── Volkov/
│   ├── suspicious.ogv
│   ├── angry.ogv
│   └── smoking.ogv
└── README.md
```

## 🎭 Разница между типами видео:

### Полноэкранные кат-сцены (`/Cutscenes/`)
- Занимают весь экран
- Прерывают диалог
- Для важных событий
- Можно пропустить
- Примеры: прибытие поезда, комната допроса

### Анимированные портреты (`/Characters/Animations/`)
- Маленькое окно (как обычный портрет персонажа)
- НЕ прерывают диалог
- Зациклены (loop)
- Для эмоций и мимики
- Примеры: нервничает, улыбается, думает

## 📐 Рекомендации по созданию:

### Разрешение портретов:
- **400x600** - стандартный портрет
- **512x768** - высокое качество
- **300x450** - низкое качество (маленький размер файла)

### Длительность:
- **2-4 секунды** - оптимально для зацикливания
- Анимация будет автоматически зациклена

### Примеры анимаций:

#### Для Данилы (агент):
- `nervous.ogv` - нервно оглядывается (для допросов)
- `confident.ogv` - уверенный взгляд
- `thinking.ogv` - задумался (принимает решение)
- `scared.ogv` - испуган (провалил миссию)
- `relief.ogv` - вздох облегчения

#### Для майора Волкова:
- `suspicious.ogv` - подозрительно смотрит
- `smoking.ogv` - курит сигару
- `angry.ogv` - злится, стучит по столу
- `laughing.ogv` - саркастически смеется

#### Для Анны:
- `smile.ogv` - милая улыбка
- `blush.ogv` - краснеет
- `laugh.ogv` - смеется
- `worried.ogv` - беспокоится

## 🛠️ Конвертация FFmpeg:

### Базовая (зацикленная анимация):
```bash
ffmpeg -i input.mp4 -c:v libtheora -qscale:v 7 -c:a libvorbis -qscale:a 5 -vf scale=400:600 nervous.ogv
```

### С обрезкой (взять 3 секунды):
```bash
ffmpeg -i input.mp4 -ss 00:00:05 -to 00:00:08 -c:v libtheora -qscale:v 7 -vf scale=400:600 nervous.ogv
```

### Из статичного изображения с эффектом:
```bash
# Лёгкое покачивание
ffmpeg -loop 1 -i portrait.png -t 3 -vf "scale=400:600,zoompan=z='zoom+0.001':d=75" -c:v libtheora nervous.ogv

# Дрожание (нервозность)
ffmpeg -loop 1 -i portrait.png -t 3 -vf "scale=400:600,random=seed=123:frames=2" -c:v libtheora nervous.ogv
```

## 🎬 Использование в сценариях:

### Вариант 1: Через команду (создадим позже)
```
danila animated_nervous "Я... я не знаю о чем вы говорите!"
```

### Вариант 2: Через параметр команды character
```
character danila nervous animated
danila "Я нервничаю..." 
```

### Вариант 3: Отдельная команда
```
animate_portrait danila nervous
danila "Что-то пошло не так..."
stop_animation danila
```

## 📦 Размер файлов:

Старайтесь, чтобы каждая анимация была **меньше 2-5 MB**.

Для коротких зацикленных анимаций это легко достижимо.

## ✅ Текущие файлы:

- `characterisnervous.ogv` - ТЕСТ (переместить в `Danila/nervous.ogv`)

## 🎯 План создания:

1. **Собрать/создать базовые анимации** для главного героя:
   - nervous.ogv (уже есть!)
   - happy.ogv
   - thinking.ogv
   - scared.ogv

2. **Для майора Волкова**:
   - suspicious.ogv (важно!)
   - smoking.ogv

3. **Для остальных персонажей** - по необходимости

## 💡 Где взять анимации:

1. **Создать в Live2D / Spine** - профессионально
2. **Анимировать в After Effects** - из статичных портретов
3. **Использовать AI** (Runway ML, Stable Diffusion) - генерация
4. **Простые эффекты в FFmpeg** - покачивание, моргание
5. **Mixamo** - для 3D персонажей

## 🚀 Следующий шаг:

Интегрировать систему в CharacterDisplayer для автоматической замены статичных портретов на анимированные!
