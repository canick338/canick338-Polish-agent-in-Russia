# Мини-игра "Расфасовка повидла" - Требования к ресурсам

## 📐 Разрешения и размеры

### Фон конвейера
- **Разрешение**: 1920x1080px (Full HD)
- **Формат**: PNG или JPG
- **Описание**: Фон завода с конвейерной лентой
- **Файл**: `conveyor_background.png`

### Банка повидла
- **Разрешение**: 80x120px (высота больше ширины)
- **Формат**: PNG с прозрачностью
- **Описание**: Банка с повидлом, вид сбоку
- **Файл**: `jam_jar.png`
- **Варианты**: Можно сделать несколько вариантов (разные цвета крышек)

### Наклейка на банку
- **Разрешение**: 80x30px
- **Формат**: PNG с прозрачностью
- **Описание**: Этикетка/наклейка для банки
- **Файл**: `jar_sticker.png`
- **Варианты**: Можно сделать несколько вариантов (разные цвета/тексты)

### Конвейерная лента
- **Разрешение**: 1920x200px (широкая полоса)
- **Формат**: PNG с прозрачностью (опционально)
- **Описание**: Текстура конвейерной ленты
- **Файл**: `conveyor_belt.png`
- **Анимация**: Можно сделать анимированную версию для движения

### UI элементы

#### Кнопка "Пропустить"
- **Разрешение**: 200x60px
- **Формат**: PNG с прозрачностью
- **Файл**: `skip_button.png`
- **Состояния**: normal, hover, pressed

#### Панель информации
- **Разрешение**: 400x150px
- **Формат**: PNG с прозрачностью
- **Описание**: Панель для таймера и счёта
- **Файл**: `info_panel.png`

## 🎨 Цветовая схема

### Основные цвета:
- **Фон завода**: Тёмно-серый/коричневый (#2a2a2a или #3d2a1a)
- **Конвейер**: Серый металл (#555555)
- **Банка**: Светло-коричневый/бежевый (#d4a574)
- **Повидло**: Оранжево-красный (#d2691e)
- **Наклейка**: Красный/жёлтый (#dc143c или #ffd700)
- **UI**: Тёмный с акцентами (#1a1a1a с #ff6b6b)

## 📁 Структура файлов

```
godot/Factory/
├── textures/
│   ├── conveyor_background.png (1920x1080)
│   ├── conveyor_belt.png (1920x200)
│   ├── jam_jar.png (80x120)
│   ├── jar_sticker.png (80x30)
│   ├── skip_button.png (200x60)
│   └── info_panel.png (400x150)
├── FactoryJamScene.tscn
├── FactoryJamScene.gd
├── FactoryJamGame.gd
└── README.md
```

## 🎮 Визуальные эффекты

### Анимации:
1. **Движение банок**: Плавное движение слева направо
2. **Наклейка**: Появление с эффектом масштабирования
3. **Пропущенная банка**: Эффект "ухода" с затемнением
4. **Таймер**: Пульсация при малом времени (< 5 секунд)

### Частицы (опционально):
- **Успех**: Звёздочки при наклеивании наклейки
- **Провал**: Красный крестик при пропуске банки

## 💡 Советы по созданию

1. **Банки**: Сделайте объёмными, с тенями и бликами
2. **Конвейер**: Добавьте текстуру металла/резины
3. **Фон**: Детализированный, но не отвлекающий
4. **UI**: Минималистичный, но информативный
5. **Наклейки**: Яркие, заметные, но не кричащие

## 🔊 Звуки

### Звук наклеивания наклейки
- **Формат**: OGG Vorbis или WAV
- **Длительность**: 0.1-0.3 секунды
- **Описание**: Короткий звук "приклеивания" или "щёлчка"
- **Файл**: `sticker_sound.ogg`
- **Разрешение**: 44.1 kHz, 16-bit, моно или стерео

### Звук конвейера
- **Формат**: OGG Vorbis (рекомендуется для зацикливания)
- **Длительность**: 2-5 секунд (будет зациклен)
- **Описание**: Фоновый звук работающего конвейера
- **Файл**: `conveyor_sound.ogg`
- **Разрешение**: 44.1 kHz, 16-bit, моно или стерео
- **Важно**: Должен быть бесшовным для зацикливания

### Структура звуков:
```
godot/Factory/
├── Sounds/
│   ├── sticker_sound.ogg
│   └── conveyor_sound.ogg
```

## ✨ Дополнительные эффекты (Juice)

### Частицы
Для улучшения визуальной отдачи можно добавить текстуры частиц:
- **Звёздочка**: `star_particle.png` (для эффекта успеха)
- **Дым**: `smoke_particle.png` (для эффекта появления/исчезновения)
- **Формат**: PNG с прозрачностью, белый цвет (красится кодом)
- **Размер**: 32x32px или 64x64px

### Шрифты
Для всплывающего текста (Combo x2, +10) рекомендуется использовать "мультяшный" шрифт:
- **Файл**: `comic_font.ttf` или `impact_font.ttf`
- **Описание**: Жирный, читаемый шрифт для цифр и коротких надписей


## 🎬 Движущийся конвейер

### Текстура конвейерной ленты

#### Основные требования:
- **Разрешение**: 1920x200px (или больше для бесшовности)
- **Формат**: PNG с прозрачностью (опционально) или JPG
- **Описание**: Текстура конвейерной ленты, которая будет двигаться
- **Файл**: `conveyor_belt.png`
- **Важно**: Текстура должна быть **бесшовной** по горизонтали для плавного зацикливания

#### Как сделать бесшовную текстуру:

##### Если у вас есть EPS файл:

1. **Конвертация EPS в PNG/JPG:**
   - **В Adobe Illustrator:**
     * Откройте EPS файл
     * Проверьте, что паттерн бесшовный (левая и правая границы совпадают)
     * File → Export → Export As
     * Выберите PNG или JPG
     * **Разрешение**: 1920x200px (или больше, например 3840x200px для детализации)
     * **DPI**: 72-150 (для игр достаточно 72)
     * **Цветовой режим**: RGB
     * Экспортируйте
   
   - **В Inkscape (бесплатно):**
     * Откройте EPS файл
     * File → Export PNG Image
     * Установите размер: 1920x200px (или больше)
     * DPI: 72-150
     * Экспортируйте
   
   - **Онлайн конвертеры:**
     * Zamzar.com, CloudConvert.com
     * Загрузите EPS, выберите PNG/JPG
     * Установите размер: 1920x200px
     * Скачайте результат

2. **Проверка бесшовности после конвертации:**
   - Откройте полученный PNG/JPG в Photoshop/GIMP
   - Используйте Filter → Other → Offset
   - Сместите на половину ширины (960px для 1920px текстуры)
   - Если видите швы - дорисуйте их, чтобы границы совпадали
   - Сохраните как PNG

##### Если создаёте с нуля:

1. **В Photoshop/GIMP:**
   - Создайте изображение нужной ширины (например, 1920px)
   - Нарисуйте паттерн конвейерной ленты (металлические пластины, резиновые полосы, линии)
   - **Критически важно**: Левая и правая границы должны идеально совпадать
   - Используйте фильтр "Offset" (Filter → Other → Offset) для проверки бесшовности
   - Если видите швы - дорисуйте их, чтобы границы совпадали

2. **Паттерн конвейерной ленты:**
   - Металлические пластины с заклёпками
   - Горизонтальные линии (имитация рёбер жёсткости)
   - Текстура резины/металла
   - Тени и блики для объёма
   - Можно добавить небольшие детали (царапины, износ)

3. **Цветовая схема:**
   - Основной цвет: Серый металл (#555555 - #777777)
   - Тени: Тёмно-серый (#333333)
   - Блики: Светло-серый (#aaaaaa)
   - Можно добавить ржавчину/износ для реалистичности

4. **Технические детали:**
   - Высота: 200px (или больше, если нужна детализация)
   - Ширина: Кратна 1920px для удобства (1920, 3840, 5760px)
   - Разрешение: 72-150 DPI (для игр достаточно)
   - Формат: PNG для прозрачности или JPG для экономии места

#### Режим отображения в Godot:
- **Texture Mode**: `STRETCH_TILE` или `STRETCH_TILE_REPEAT`
- Это позволяет текстуре повторяться бесконечно по горизонтали
- Настройка в Inspector: Texture → Flags → Repeat (включить)

### Анимация конвейера

#### Техническая реализация:

1. **Скорость движения:**
   - Настраивается в коде (по умолчанию 100-200 пикселей/сек)
   - Можно сделать переменной (ускорение со временем)
   - Рекомендуемая скорость: 150-250 px/sec для реалистичности

2. **Направление:**
   - Справа налево (имитация движения ленты)
   - Банки движутся вместе с лентой

3. **Реализация в Godot:**
   ```gdscript
   # Создание TextureRect для конвейера
   var conveyor_texture = TextureRect.new()
   conveyor_texture.texture = load("res://Factory/Textures/conveyor_belt.png")
   conveyor_texture.stretch_mode = TextureRect.STRETCH_TILE
   conveyor_texture.texture_repeat = true
   
   # Анимация через смещение текстуры
   var offset = 0.0
   func _process(delta):
       offset += speed * delta
       # Используем material shader или смещение UV координат
       conveyor_texture.material.set_shader_parameter("offset", offset)
   ```

4. **Альтернативный способ (через Sprite2D):**
   ```gdscript
   # Создание Sprite2D с повторяющейся текстурой
   var conveyor_sprite = Sprite2D.new()
   conveyor_sprite.texture = load("res://Factory/Textures/conveyor_belt.png")
   conveyor_sprite.region_enabled = true
   conveyor_sprite.region_rect = Rect2(0, 0, 1920, 200)
   
   # Анимация через смещение region_rect
   func _process(delta):
       var offset = int(speed * delta) % texture_width
       conveyor_sprite.region_rect.position.x = offset
   ```

5. **Синхронизация с банками:**
   - Банки должны двигаться с той же скоростью, что и конвейер
   - Это создаёт иллюзию, что банки стоят на ленте
   - Используйте одинаковую скорость для всех элементов

#### Визуальные эффекты:

1. **Тени и объём:**
   - Добавьте тень под конвейером для глубины
   - Можно использовать ColorRect с градиентом

2. **Пыль/частицы (опционально):**
   - Небольшие частицы пыли над конвейером
   - Создают атмосферу завода

3. **Освещение:**
   - Блики на металлических частях
   - Могут двигаться вместе с лентой

### Проверка качества:

#### Реализация анимации в коде:

В `FactoryJamScene.gd` уже реализована анимация конвейера через смещение `offset_left` и `offset_right`:

```gdscript
# В _process():
if conveyor_texture and game_logic.current_state == FactoryJamGame.GameState.PLAYING:
    conveyor_offset += conveyor_speed * delta
    
    if conveyor_texture.texture:
        var texture_width = conveyor_texture.texture.get_width()
        if texture_width > 0:
            conveyor_offset = fmod(conveyor_offset, texture_width)
            conveyor_texture.offset_left = -conveyor_offset
            conveyor_texture.offset_right = conveyor_offset
```

**Как это работает:**
- `conveyor_offset` увеличивается каждый кадр на `conveyor_speed * delta`
- Когда offset превышает ширину текстуры, он сбрасывается через `fmod()` (модуло)
- `offset_left = -conveyor_offset` и `offset_right = conveyor_offset` создают эффект смещения текстуры
- Благодаря бесшовности текстуры и режиму `STRETCH_TILE`, движение выглядит непрерывным

**Настройка скорости:**
```gdscript
var conveyor_speed: float = 100.0  # Пикселей в секунду
# Рекомендуется: 150-250 для реалистичного движения
# Должна совпадать с jar_speed для синхронизации
```

**Важно для синхронизации:**
- Скорость конвейера (`conveyor_speed`) должна совпадать со скоростью банок (`jar_speed`)
- Оба значения должны быть одинаковыми (например, оба 200.0)
- Это создаёт иллюзию, что банки стоят на движущейся ленте

#### Пошаговая инструкция создания:

##### Если у вас есть EPS файл (рекомендуется):

1. **Конвертация EPS в PNG:**
   - **В Adobe Illustrator:**
     * Откройте EPS файл
     * **Проверьте бесшовность**: Убедитесь, что левая и правая границы паттерна совпадают
     * File → Export → Export As
     * Выберите PNG
     * **Размер**: 1920x200px (или 3840x200px для большей детализации)
     * **DPI**: 72-150 (для игр достаточно 72)
     * **Цветовой режим**: RGB
     * **Формат**: PNG-24 (для качества)
     * Экспортируйте
   
   - **В Inkscape (бесплатно):**
     * Откройте EPS файл (File → Open)
     * Проверьте размеры документа (Object → Document Properties)
     * File → Export PNG Image
     * Установите размер: 1920x200px (или больше)
     * DPI: 72-150
     * Нажмите "Export"
   
   - **Онлайн конвертеры (если нет Illustrator/Inkscape):**
     * Zamzar.com, CloudConvert.com, Convertio.co
     * Загрузите EPS файл
     * Выберите формат: PNG
     * Установите размер: 1920x200px
     * Скачайте результат

2. **Проверка бесшовности после конвертации:**
   - Откройте полученный PNG в Photoshop/GIMP
   - Filter → Other → Offset
   - Сместите на половину ширины (960px для 1920px текстуры)
   - Если видите швы - вернитесь в Illustrator/Inkscape:
     * Исправьте паттерн, чтобы левая и правая границы совпадали
     * Повторно экспортируйте
   - Если швов нет - текстура готова!

3. **Сохранение:**
   - Сохраните как PNG (для качества) или JPG (для экономии места)
   - Поместите в `godot/Factory/Textures/conveyor_belt.png`

##### Если создаёте с нуля:

1. **Создание текстуры:**
   - Откройте Photoshop/GIMP/Krita
   - Создайте новый документ: 1920x200px, 72 DPI, RGB
   - Нарисуйте паттерн конвейерной ленты:
     * Металлические пластины с заклёпками
     * Горизонтальные линии (рёбра жёсткости)
     * Текстура резины/металла
     * Тени и блики для объёма
   - **Критически важно**: Используйте Filter → Other → Offset (960px по горизонтали)
   - Дорисуйте швы, чтобы левая и правая части идеально совпадали
   - Сохраните как PNG (для качества) или JPG (для экономии места)

2. **Импорт в Godot:**
   - Поместите файл в `godot/Factory/Textures/conveyor_belt.png`
   - Выберите файл в FileSystem
   - В Inspector → Import установите:
     * **Repeat**: Enabled (для зацикливания текстуры)
     * **Filter**: Enabled (для сглаживания)
     * **Mipmaps**: Enabled (для оптимизации)
   - Нажмите "Reimport"

3. **Подключение в коде:**
   - В `FactoryJamScene.gd` найдите функцию `_create_conveyor()`
   - Раскомментируйте строки:
   ```gdscript
   var conveyor_img = load("res://Factory/Textures/conveyor_belt.png")
   if conveyor_img:
       conveyor_texture.texture = conveyor_img
   ```
   - Убедитесь, что `conveyor_texture.stretch_mode = TextureRect.STRETCH_TILE`
   - Проверьте, что `conveyor_speed` установлена (100-250 px/sec)

4. **Тестирование:**
   - Запустите игру и проверьте движение конвейера
   - Убедитесь, что нет видимых швов при движении
   - Проверьте синхронизацию с банками (они должны двигаться вместе)
   - Настройте скорость при необходимости

#### Проверка качества:

1. **Бесшовность:**
   - Приложите левую границу к правой - они должны идеально совпадать
   - Используйте инструмент "Offset" в графическом редакторе (Filter → Other → Offset)
   - При движении в игре не должно быть видно "швов" или "прыжков"
   - Если видите швы - дорисуйте их в графическом редакторе

2. **Скорость:**
   - Движение должно быть плавным, без рывков
   - Проверьте на разных FPS (30, 60, 120)
   - Убедитесь, что банки движутся синхронно с лентой
   - Скорость конвейера должна совпадать со скоростью банок

3. **Производительность:**
   - Текстура не должна быть слишком большой (макс 4K = 4096px ширина)
   - Используйте сжатие для оптимизации (PNG сжатие или JPG качество 85-90%)
   - Проверьте FPS во время игры (должно быть стабильно 60 FPS)
   - Рекомендуемый размер: 1920x200px (или 3840x200px для большей детализации)

## 🔧 Технические требования

### Текстуры:
- **Формат**: PNG для элементов с прозрачностью, JPG для фонов
- **Сжатие**: Оптимальное (не максимальное) для качества
- **Цветовой профиль**: sRGB
- **Глубина цвета**: 24-bit (RGB) или 32-bit (RGBA)

### Звуки:
- **Формат**: OGG Vorbis (рекомендуется) или WAV
- **Частота дискретизации**: 44.1 kHz
- **Битность**: 16-bit
- **Каналы**: Моно или стерео
- **Сжатие**: Для OGG используйте качество 4-6 (баланс размер/качество)

## 🎨 Анимация наклеивания наклейки

### Визуальные эффекты:
1. **Появление наклейки**: 
   - Начинает сверху банки (position.y = -20)
   - Плавно опускается вниз (0.2 сек)
   - Одновременно появляется (fade in) и увеличивается (scale 0.5 → 1.1 → 1.0)
   - Эффект "отскока" при приклеивании

2. **Звуковое сопровождение**:
   - Короткий звук при клике
   - Можно добавить несколько вариантов для разнообразия

3. **Визуальная обратная связь**:
   - Вспышка при клике (жёлтый цвет)
   - Увеличение банки (scale 1.2 → 1.0)
   - Эффект "частиц" (опционально)

## 📝 Интеграция ресурсов

После создания ресурсов:

1. **Текстуры**: Поместите в `godot/Factory/Textures/`
2. **Звуки**: Поместите в `godot/Factory/Sounds/`
3. **Обновите код**: 
   - В `FactoryJamScene.gd` раскомментируйте строки загрузки текстур и звуков
   - Настройте пути к файлам

Пример:
```gdscript
# В _create_audio_players():
sticker_sound.stream = load("res://Factory/Sounds/sticker_sound.ogg")
conveyor_sound.stream = load("res://Factory/Sounds/conveyor_sound.ogg")

# В _create_conveyor():
var conveyor_img = load("res://Factory/Textures/conveyor_belt.png")
if conveyor_img:
    conveyor_texture.texture = conveyor_img
```

