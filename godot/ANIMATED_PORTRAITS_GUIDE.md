# 🎭 Руководство по системе анимированных портретов

## 📚 Оглавление

1. [Два типа видео](#два-типа-видео)
2. [Быстрый старт](#быстрый-старт)
3. [Создание анимаций](#создание-анимаций)
4. [Примеры использования](#примеры-использования)
5. [Структура папок](#структура-папок)

---

## Два типа видео

### 🎬 Полноэкранные кат-сцены

**Расположение:** `godot/Cutscenes/`

**Использование:**
```
cutscene video_name
```

**Характеристики:**
- Занимают весь экран
- Прерывают диалог
- Можно пропустить (по умолчанию)
- Для важных событий

**Примеры:**
- Прибытие поезда
- Комната допроса
- Концовки игры

---

### 🎭 Анимированные портреты

**Расположение:** `godot/Characters/Animations/{Character}/`

**Использование:**
```
danila nervous "Я нервничаю!"
```

**Характеристики:**
- Размер как обычный портрет (400x600)
- НЕ прерывают диалог
- Автоматически зациклены
- Автоматически используются если файл существует

**Примеры:**
- Нервозность
- Улыбка
- Моргание
- Дыхание

---

## Быстрый старт

### Шаг 1: Создать анимацию

```bash
ffmpeg -i input.mp4 -ss 00:00:05 -to 00:00:08 -c:v libtheora -qscale:v 7 -vf scale=400:600 nervous.ogv
```

### Шаг 2: Поместить в правильную папку

```
godot/Characters/Animations/Danila/nervous.ogv
```

### Шаг 3: Использовать в сценарии

```
danila nervous "Текст"
```

**Готово!** Система автоматически покажет анимацию.

---

## Создание анимаций

### Из видео файла

```bash
# Базовая конвертация
ffmpeg -i input.mp4 -c:v libtheora -qscale:v 7 -vf scale=400:600 output.ogv

# С обрезкой (взять с 5 по 8 секунду)
ffmpeg -i input.mp4 -ss 00:00:05 -to 00:00:08 -c:v libtheora -qscale:v 7 -vf scale=400:600 output.ogv

# Высокое качество
ffmpeg -i input.mp4 -c:v libtheora -qscale:v 9 -vf scale=512:768 output.ogv

# Маленький размер
ffmpeg -i input.mp4 -c:v libtheora -qscale:v 5 -vf scale=300:450 output.ogv
```

### Из статичного изображения

```bash
# Лёгкое покачивание (дыхание)
ffmpeg -loop 1 -i portrait.png -t 3 -vf "scale=400:600,zoompan=z='zoom+0.001':d=75" -c:v libtheora output.ogv

# Зум эффект
ffmpeg -loop 1 -i portrait.png -t 3 -vf "scale=400:600,zoompan=z='min(zoom+0.0015,1.5)':d=75:x='iw/2-(iw/zoom/2)':y='ih/2-(ih/zoom/2)'" -c:v libtheora output.ogv

# Моргание (требует последовательность изображений)
ffmpeg -framerate 10 -i frame_%03d.png -c:v libtheora -vf scale=400:600 blink.ogv
```

### AI генерация

1. **Runway ML** - генерация видео из изображений
2. **Stable Diffusion + AnimateDiff** - анимация
3. **Live2D** - профессиональная 2D анимация
4. **Spine** - скелетная анимация

---

## Примеры использования

### Базовые примеры

```
# Автоматическая анимация
danila nervous "Я волнуюсь..."

# Статичный портрет (если анимации нет)
danila neutral "Спокойный текст"

# С анимацией входа
danila happy enter "Привет!"

# Разные стороны
anna smile left "Как дела?"
danila nervous right "Х-хорошо..."
```

### Переключение эмоций

```
danila neutral "Сначала я спокоен."
danila nervous "Потом начинаю нервничать..."
danila scared "А теперь испуган!"
danila relief "Фух, пронесло."
```

### Во время допроса

```
volkov suspicious "Расскажите о себе..."

if suspicion > 50:
	danila nervous "Я... обычный человек!"
else:
	danila confident "Данила Петров, слесарь."
	
volkov suspicious "Интересно..."
```

### В миссиях

```
danila thinking "Нужно сфотографировать документы..."

choice:
	"Рискнуть":
		danila scared "Кто-то идёт!"
		set suspicion +20
	"Отступить":
		danila relief "Слишком опасно..."
		set stealth +10
```

---

## Структура папок

```
godot/
├── Cutscenes/                          # Полноэкранные видео
│   ├── characterisnervous.ogv          # Тестовое видео
│   ├── prologue_intro.ogv
│   ├── arrival_train.ogv
│   └── ending_arrested.ogv
│
├── Characters/
│   ├── Animations/                     # Анимированные портреты
│   │   ├── Danila/
│   │   │   ├── nervous.ogv ✅
│   │   │   ├── happy.ogv
│   │   │   ├── thinking.ogv
│   │   │   ├── scared.ogv
│   │   │   ├── confident.ogv
│   │   │   └── relief.ogv
│   │   ├── Anna/
│   │   │   ├── smile.ogv
│   │   │   ├── blush.ogv
│   │   │   └── laugh.ogv
│   │   ├── Volkov/
│   │   │   ├── suspicious.ogv
│   │   │   ├── smoking.ogv
│   │   │   └── angry.ogv
│   │   └── README.md
│   │
│   ├── Dani/                          # Статичные изображения
│   │   ├── dani_neutral.png
│   │   ├── dani_happy.png
│   │   └── ...
│   │
│   ├── AnimatedCharacterPortrait.gd   # Компонент анимации
│   ├── AnimatedCharacterPortrait.tscn
│   └── CharacterDisplayer.gd          # Система отображения
│
└── Story/
	└── cutscenes/
		├── test_animated_portraits.txt # Тестовый сценарий
		└── CUTSCENE_USAGE.md
```

---

## Технические детали

### Требования к файлам

| Параметр | Рекомендуется | Диапазон |
|----------|---------------|----------|
| Разрешение | 400x600 | 300x450 - 512x768 |
| Длительность | 2-4 сек | 1-5 сек |
| Формат | .ogv | .ogv, .webm |
| Размер | 2-5 MB | до 10 MB |
| FPS | 24-30 | 15-60 |

### Автоматическое определение

Система проверяет файлы в следующем порядке:

1. `Characters/Animations/{CharacterID}/{expression}.ogv`
2. `Characters/Animations/{CharacterID}/{expression}.webm`
3. Если не найдено → использует статичное изображение

Пример:
```
danila nervous "Text"
```

Ищет:
1. `Characters/Animations/Danila/nervous.ogv` ✅
2. `Characters/Animations/Danila/nervous.webm`
3. Fallback: `Characters/Dani/dani_nervous.png`

---

## Отладка

### Проблема: Анимация не показывается

**Проверьте:**

1. Файл существует:
   ```
   godot/Characters/Animations/Danila/nervous.ogv
   ```

2. ID персонажа правильный:
   ```gdscript
   # В файле marek.tres или danila.tres
   id = "danila"  # С маленькой буквы
   ```

3. Название эмоции совпадает:
   ```
   danila nervous "..."  # nervous.ogv
   ```

4. Формат поддерживается (`.ogv` или `.webm`)

### Проблема: Анимация не зацикливается

В `AnimatedCharacterPortrait.gd` проверьте:
```gdscript
video_player.loop = true  # Должно быть true
```

### Проблема: Анимация слишком большая/маленькая

Измените разрешение при конвертации:
```bash
# Меньше
ffmpeg -i input.mp4 -vf scale=300:450 ...

# Больше
ffmpeg -i input.mp4 -vf scale=512:768 ...
```

---

## Производительность

### Оптимизация

1. **Используйте .ogv** вместо .webm (лучше совместимость)
2. **Размер файла < 5 MB** для каждой анимации
3. **Разрешение 400x600** оптимально
4. **Длительность 2-3 секунды** для зацикливания
5. **FPS 24-30** достаточно

### Рекомендации

- ✅ Анимации для главных персонажей (Danila, Anna, Volkov)
- ✅ Важные эмоции (nervous, scared, happy)
- ⚠️ Второстепенные персонажи - по необходимости
- ❌ Не создавайте анимации для всех эмоций всех персонажей

---

## Примеры для вашей игры

### Данила (агент)

```
# Допрос
volkov suspicious "Где вы были вчера вечером?"
danila nervous "Я... я был дома!"

# Успешная миссия
danila confident "Документы получены."
danila relief "Никто не заметил."

# Провал
danila scared "Они знают!"
```

### Анна (любовный интерес)

```
anna smile "Привет, Данила!"
danila nervous "П-привет..."
anna blush "Хочешь прогуляться?"
```

### Майор Волков

```
volkov suspicious "Что-то в вас не так..."
volkov smoking "Расскажите правду..."
volkov angry "Вы лжёте!"
```

---

## Следующие шаги

1. ✅ Скопировать `characterisnervous.ogv` → `Danila/nervous.ogv`
2. ⏳ Создать `happy.ogv`, `thinking.ogv`, `scared.ogv`
3. ⏳ Добавить анимации для Волкова (suspicious, smoking)
4. ⏳ Интегрировать в основные сценарии
5. ⏳ Создать анимации для Анны

---

## Ссылки

- [Основной гайд по кат-сценам](../CUTSCENE_GUIDE.md)
- [Примеры использования](../Story/cutscenes/CUTSCENE_USAGE.md)
- [Тестовый сценарий](../Story/cutscenes/test_animated_portraits.txt)
