# Инструкция по публикации на GitHub

## 🚀 Быстрая публикация

Откройте **обычный терминал** (не встроенный в Cursor) и выполните:

```bash
# 1. Перейти в папку проекта
cd "C:\Users\rushe\OneDrive\Desktop\Code\godot-2d-visual-novel-main"

# 2. Проверить статус git
git status

# 3. Добавить remote (если еще не добавлен)
git remote add origin https://github.com/canick338/canick338-Polish-agent-in-Russia.git

# Если remote уже существует, удалить и добавить заново:
# git remote remove origin
# git remote add origin https://github.com/canick338/canick338-Polish-agent-in-Russia.git

# 4. Добавить все файлы
git add .

# 5. Сделать коммит
git commit -m "Добавлена игра про польского агента в России с казино и системой миссий"

# 6. Переименовать ветку в main (если нужно)
git branch -M main

# 7. Запушить на GitHub
git push -u origin main
```

## 📋 Пошаговая инструкция

### Шаг 1: Проверка git
```bash
git --version
```
Если git не установлен, скачайте с: https://git-scm.com/

### Шаг 2: Проверка текущего состояния
```bash
git status
```

### Шаг 3: Проверка remote
```bash
git remote -v
```

Если remote уже существует с другим URL:
```bash
git remote set-url origin https://github.com/canick338/canick338-Polish-agent-in-Russia.git
```

### Шаг 4: Добавление файлов
```bash
git add .
```

Проверить что добавлено:
```bash
git status
```

### Шаг 5: Коммит
```bash
git commit -m "Initial commit: Polish agent spy game with casino and mission system"
```

### Шаг 6: Пуш
```bash
git branch -M main
git push -u origin main
```

## ⚠️ Возможные проблемы

### Проблема 1: Remote уже существует
**Ошибка:** `remote origin already exists`

**Решение:**
```bash
git remote remove origin
git remote add origin https://github.com/canick338/canick338-Polish-agent-in-Russia.git
```

### Проблема 2: Нужна аутентификация
**Ошибка:** `Authentication failed`

**Решение:**
1. Настроить GitHub token
2. Или использовать GitHub Desktop

### Проблема 3: Большой размер файлов
**Ошибка:** `file is too large`

**Решение:**
```bash
# Проверить большие файлы
git ls-files --cached -z | xargs -0 du -h | sort -rh | head -20

# Удалить из tracking
git rm --cached путь/к/большому/файлу
```

### Проблема 4: Конфликты
**Ошибка:** `conflict`

**Решение:**
```bash
# Принять локальную версию
git push -f origin main
```

## 🔒 Настройка GitHub Token (если нужно)

1. Зайти на GitHub: Settings → Developer settings → Personal access tokens
2. Generate new token (classic)
3. Выбрать права: `repo` (все)
4. Скопировать token
5. При пуше использовать token вместо пароля

## ✅ Проверка после публикации

После успешного пуша проверьте:
1. Зайдите на https://github.com/canick338/canick338-Polish-agent-in-Russia
2. Проверьте что все файлы загружены
3. README.md должен отображаться на главной странице

## 📝 Рекомендации

### Перед публикацией:
- ✅ .gitignore создан
- ✅ README.md создан
- ⚠️ Проверьте что нет личных данных в файлах
- ⚠️ Проверьте что нет больших бинарных файлов (> 100MB)

### После публикации:
- 📝 Добавить описание репозитория на GitHub
- 🏷️ Добавить теги: `godot`, `visual-novel`, `spy-game`, `russian`
- 📄 Добавить лицензию через GitHub UI
- 🖼️ Добавить скриншоты в README

## 🎮 Альтернативный метод: GitHub Desktop

1. Скачать GitHub Desktop: https://desktop.github.com/
2. File → Add Local Repository
3. Выбрать папку проекта
4. Publish Repository
5. Выбрать имя: `canick338-Polish-agent-in-Russia`
6. Нажать Publish

---

**Удачи с публикацией! 🚀**










